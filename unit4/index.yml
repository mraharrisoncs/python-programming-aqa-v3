# yaml-language-server: $schema=https://vscodeedu.com/assets/schema/course-v2.schema.json
id: unit4
title: Unit 4 - Selection
description: |-
  Programming selection in Python 
  (also known as branching or conditional execution)
objectives: |-
  1. Understand why we need selection
  2. Boolean expressions
  3. Using if and if-else
  4. Logical operators and, or and not, and operator precedence
  5. Nested if statements and if-elif-else
  6. Simplifying if-elif-else with match/case
lessons:
  - lesson1/index.yml
  - lesson2/index.yml
  - lesson3/index.yml
  - lesson4/index.yml
